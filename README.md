My attempt at making a side scrolling shooter game, possibly a moba.

Requirements
------------

* node
* gulp - `npm install -g gulp`
* bower - `npm install -g bower`

Install
-------

```shell
npm install
bower install
```
